# Tuple bersifat immutable (tidak bisa di append,insert,pop,remove,dan ubah)
# Tuple pada dasarnya sama dengan list akan tetapi tuple tidak bisa dimodifikasi
# Tuple digunakan untuk menghindari terjadinya perubahan pada list
# Tuple ditulis menggunakan parenthesis ("()")
# Sedangkan lists ditulis menggunakan square bracket ("[]")
numbers = (1,2,3)
# Perintah dibawah tidak akan bekerja pada tuple dan mengakibatkan program menjadi error
# numbers[0]=10
# fungsi print index bisa digunakan pada tuple
print(numbers[0])

